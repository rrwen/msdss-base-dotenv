Quick Start
===========

Initialize your env file and key in a command line terminal:

>>> msdss-dotenv init

Set env variable:

>>> msdss-dotenv set USER msdss

Remove env variables:

>>> msdss-dotenv del USER

Remove env file and key:

>>> msdss-dotenv clear

Get help:

>>> msdss-dotenv --help